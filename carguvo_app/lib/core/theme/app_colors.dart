import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFFF97316);
  static const Color primaryLight = Color(0xFFFFF7ED);
  static const Color primaryDark = Color(0xFFEA580C);

  // Secondary
  static const Color secondary = Color(0xFF1E3A5F);
  static const Color secondaryLight = Color(0xFF2E5A8F);

  // Background & Surface
  static const Color background = Color(0xFFF5F7FA);
  static const Color card = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFFFFFFF);

  // Semantic
  static const Color success = Color(0xFF16A34A);
  static const Color successLight = Color(0xFFDCFCE7);
  static const Color warning = Color(0xFFF59E0B);
  static const Color warningLight = Color(0xFFFEF3C7);
  static const Color error = Color(0xFFDC2626);
  static const Color errorLight = Color(0xFFFEE2E2);
  static const Color info = Color(0xFF0EA5E9);

  // Text
  static const Color textPrimary = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textHint = Color(0xFF94A3B8);

  // Status Colors
  static const Color statusPending = Color(0xFF94A3B8); // Xám
  static const Color statusLoaded = Color(0xFF3B82F6); // Xanh dương
  static const Color statusDelivered = Color(0xFF16A34A); // Xanh lá
  static const Color statusFailed = Color(0xFFDC2626); // Đỏ
  static const Color statusReturned = Color(0xFFF59E0B); // Vàng

  // Misc
  static const Color divider = Color(0xFFE2E8F0);
  static const Color shadow = Color(0x0D000000);
  static const Color shimmer = Color(0xFFE2E8F0);
}

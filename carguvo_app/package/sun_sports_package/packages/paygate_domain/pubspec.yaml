name: paygate_domain
description: >-
  LUẬT THÀNH-CÔNG + result model cho endpoint `paygate` (NẠP/RÚT — TIỀN THẬT)
  DÙNG CHUNG app Flutter + jaspr_web (Round 7 — C1). Pure Dart, 0 dependency.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.9.2 <4.0.0"

dev_dependencies:
  test: ^1.24.0
  lints: ^5.0.0

name: app_i18n
description: >-
  Chuỗi hiển thị (tiếng Việt) DÙNG CHUNG cho app Flutter và jaspr_web. Pure
  Dart, chỉ toàn `static const String` — không phụ thuộc nền tảng.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.9.2 <4.0.0"

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0

name: sport_notice
description: "Match notice detection (goal/card/corner) shared between app Flutter + jaspr_web."
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.9.2 <4.0.0"

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0